########################
### Default settings ###
########################

# Log time with each message.
# Default: no
LogTime yes

# Log additional information about the infected file, such as its
# size and hash, together with the virus name.
ExtendedDetectionInfo yes

# Run as another user (clamd must be started by root for this option to work)
# Default: don't drop privileges
User clamav

# Maximum depth directories are scanned at.
# Default: 15
MaxDirectoryRecursion 20

DetectPUA yes
HeuristicAlerts yes
ScanPE yes
ScanELF yes
ScanOLE2 yes
ScanPDF yes
ScanSWF yes
ScanXMLDOCS yes
ScanHWP3 yes
ScanOneNote yes
ScanMail yes
ScanHTML yes
ScanArchive yes
Bytecode yes
AlertBrokenExecutables yes
AlertBrokenMedia yes
AlertEncrypted yes
AlertEncryptedArchive yes
AlertEncryptedDoc yes
AlertOLE2Macros yes
AlertPartitionIntersection yes

# Use local socket
LocalSocket /run/clamav/clamd.ctl

############################
### Real-time protection ###
############################

# Exclude the UID of the scanner itself from checking, to prevent loops
OnAccessExcludeUname clamav

# Set the mount point where to recursively perform the scan,
# this could be every path or multiple path (one line for path)
# OnAccessMountPath /mnt

# Alternatively, add some directories instead of mount points
OnAccessIncludePath /mnt
OnAccessIncludePath /home/daniel/Downloads
OnAccessIncludePath /home/daniel/aur-packages
OnAccessIncludePath /home/daniel/dwhelper
OnAccessIncludePath /home/daniel/.local/share/Steam/steamapps/common/Valheim/
OnAccessIncludePath /home/daniel/.local/share/Steam/steamapps/common/Kerbal Space Program/

# Prevention doesn't work with OnAccessMountPath.
# It works with OnAccessIncludePath, as long as /usr and /etc are not included.
# Including /var while activating prevention is also not recommended, because
# this would slow down package installation by a factor of 1000.
OnAccessPrevention no

# Perform scans on newly created, moved, or renamed files
OnAccessExtraScanning yes

# Optionallyexclude root-owned processes
# OnAccessExcludeRootUID true

######################
### Desktop alerts ###
######################

VirusEvent /etc/clamav/virus-event.bash
